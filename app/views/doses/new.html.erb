<div class="container">
<h1>Add <%= @cocktail.name %> ingredients</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: Ingredient.all, prompt: "Ingredient" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
</div>
